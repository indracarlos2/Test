@model IEnumerable<RestorationStore.Domain.Model.Respons>

@{
    ViewBag.Title = "Completed Requests";
}

<h2>Completed Requests</h2>

<table>
    <tr>
        <th>
           
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Request.Title.TitleMessage)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FinalDate)
        </th>
        
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @if(item.Id != null) {
                <div style="float:left;margin-right:20px">
                    <img width="75" height="75"
                         src="@Url.Action("GetImage",
                                    "Response",
                        new { id=item.Id })" />
                </div>
            }
        </td>
        <td>
            @Html.ActionLink(item.Request.Title.TitleMessage,
               "DetailResponse", "Response", new { id = item.Id },null)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FinalDate)
        </td>
    </tr>
}

</table>
